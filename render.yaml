services:
  - type: web
    name: fortress-intelligence-backend
    env: python
    rootDir: web_platform/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
